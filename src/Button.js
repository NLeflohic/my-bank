import React from "react";

const Button = () => {
    return (
        <div className="see-more" children={<button className="button-more" children="See More" />} />
    );
};

export default Button;